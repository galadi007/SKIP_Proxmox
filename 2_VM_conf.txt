# Auf der Ubuntu-VM ausführen
sudo apt update
sudo apt install -y openssh-server qemu-guest-agent

sudo systemctl enable --now ssh
sudo systemctl enable --now qemu-guest-agent

sudo ss -lntp | grep ':22'

# Vom Admin-Rechner ausführen. Die zentrale Konfiguration liegt im Repository.
source apps/core/cluster-host/skip-settings.conf
ssh -i "$SKIP_SSH_PRIVATE_KEY_FILE" "$SKIP_SSH_USER@$SKIP_SERVER_IP"
